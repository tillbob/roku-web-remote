# Backend Server
PORT=3000
NODE_ENV=development

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://192.168.1.*

# Optional: Default Roku IP (can be overridden in UI)
DEFAULT_ROKU_IP=

# Device Discovery
DISCOVERY_TIMEOUT=5000
MAX_DISCOVERY_DEVICES=10
